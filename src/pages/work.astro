---
import { getCollection } from 'astro:content';

import Layout from '../layouts/Layout.astro';

import PortfolioPreview from '../components/PortfolioPreview.astro';
import Hero from '../components/Hero.astro';
import Grid from '../components/Grid.astro';

const projects = (await getCollection('work')).sort(
	(a, b) => b.data.publishDate.valueOf() - a.data.publishDate.valueOf(),);
---

<Layout
	title="My Work | Angela Jasso"
	description="Learn about Angela Jasso's most recent projects">
	<div class="stack gap-20">
		<main class="wrapper stack gap-8">
			<Hero
				title="My Work"
				align="start"
			/>
			<p>"Once upon a time, someone heard about blockchain, a revolutionary technology with promising opportunities. Intrigued, they became a Blockchain Developer.</p>
			<p>They embarked on a learning journey, starting with logic programming and creating projects to apply what they learned...</p>
			<Grid variant="offset">
				{
					projects.map((project) => (
						<li>
							<PortfolioPreview project={project} />
						</li>
					))
				}
			</Grid>
			<p>They proved that anyone can master technology and use it to make the world a better place."</p>
		</main>
	</div>
</Layout>

<style>
	p {
		font-size: 1.5rem;
	}
</style>